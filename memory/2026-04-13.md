# 2026-04-13

## Summary

- Pulled `Hearth` main branch and verified it is already up to date with `origin/main`.
- Updated `ai-governance-framework` submodule working tree from `ef34729` to `48fafea` (latest reachable on `gavin0099/main`).
- Ran canonical audit trend checks for:
  - Scenario A: current `Hearth` consuming-repo path
  - Scenario B: fixture-based simulated project root under framework repo
- Published test report:
  - `docs/canonical-audit-test-report-2026-04-13.md`

## Validation Notes

- Scenario A trend currently shows high missing-signal ratio due to absent closeout/test artifacts.
- Repo-local governance docs exist, but `governance/gate_policy.yaml` is missing, so hook falls back to framework default gate policy.
- Scenario B fixture confirmed threshold behavior around `signal_threshold_ratio=0.5` (`signal_ratio=0.5455` => adoption risk true).

## UI Hotfix Notes

- Fixed web loading deadlock during network failures by converting thrown `apiFetch` exceptions into `status:error` responses in:
  - `apps/web/src/lib/accounts.ts`
  - `apps/web/src/lib/portfolio.ts`
- This prevents `ImportPanel`, `PortfolioPanel`, and `GmailSyncPanel` from staying in perpetual loading state when backend fetch throws.
