# 2026-03-23

- Re-checked current progress before continuing:
  - 2026-03-22 completed the credit-card CSV import slice.
  - credit-card PDF parsing was still only a frontend Gmail/PDF prototype, not a hardened ingestion slice.
- Continued the credit-card PDF path instead of portfolio work after confirming user intent.
- Added shared PDF credit-card parser module:
  - added `packages/shared/src/pdf-credit-card.ts`
  - parser is now a pure shared function instead of ad hoc regex only inside the web app
  - supports Sinopac and Esun statement text through explicit bank-specific parsing rules
- Parser contract for this slice:
  - infer statement year from bill header when transaction lines use `MM/DD`
  - support `MM/DD` and bank-specific second date columns
  - Sinopac parser now understands:
    - `消費日 / 入帳日 / 卡末四(可空) / 說明 / 金額 / APR / 未到期金額`
    - cashback-offset rows as positive amounts
    - installment rows using current booked amount instead of trailing remaining balance
    - autopay rows skipped
    - foreign-currency helper tails like `02/10 USD21.000` stripped from description while keeping TWD booked amount
    - negative booked TWD rows treated as reversals/refunds instead of forced expenses
    - zero-amount point-offset rows skipped
  - Esun parser now understands:
    - `本期費用明細` and `本期消費明細` as active parse sections
    - `卡號：...` headers and summary blocks as non-transaction rows
    - cashback rows as positive amounts
    - installment rows using the final booked amount column
- Frontend wiring:
  - `apps/web/src/lib/pdf-parser.ts` now delegates Sinopac/Esun parsing to the shared module
- Verification notes:
  - added parser regression tests in `apps/api/tests/pdf-credit-card.test.ts`
  - full `npm --workspace @hearth/api run test` could not complete in this sandbox because `node --test --import tsx` hit `spawn EPERM`
  - `npm --workspace @hearth/web run check` and `npm --workspace @hearth/api run build` could not run because local `node_modules` / `tsc` are not present in this sandbox
  - performed direct parser execution with `node --experimental-strip-types` and confirmed expected outputs for:
    - statement-year inference
    - Sinopac cashback / autopay / installment-current-amount behavior
    - Sinopac foreign-currency tail stripping and negative reversal handling
    - Esun section gating / cashback / installment-current-amount behavior
    - payment-line skipping
- Risk note:
  - parser remains text-layout dependent because PDF extraction order still comes from `pdfjs-dist`
  - next hardening step should use one or two real statement samples to lock bank-specific line shapes and line-break anomalies
- Adjusted the combined credit-card ledger behavior after user validation:
  - Gmail/PDF imports now keep bank-specific transaction sources (`gmail_pdf_sinopac` / `gmail_pdf_esun`)
  - the combined credit-card ledger should prefer those sources for its visible label instead of relying only on the target account name
  - the ledger account filter should be scoped to `cash_credit` accounts so the view behaves as a cross-bank credit-card ledger
- Refined the Gmail credit-card import UX after user feedback:
  - removed per-email account selection from the Gmail sync UI
  - Gmail sync now auto-resolves an internal `cash_credit` account and treats account linkage as storage-only, not a user-facing concept
  - the intended user model is now "one combined credit-card expense view with bank/card label separation", not "one ledger per account"
- Further aligned the ledger UX with that model:
  - removed the account filter from the credit-card ledger page
  - the ledger now always shows all credit-card spending together and relies on the visible bank label instead of account-based navigation
