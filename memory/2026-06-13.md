# 2026-06-13

## Gmail sync reconciliation fix
- 新增 `review_reason` 欄位至 `import_jobs`（migration 20260613000001）
- Cron 改用 INSERT + 23505 conflict 偵測，`needs_review + missing_mapping` 可在 mapping 建立後由 cron 自動升級為 `pending_parse`
- `handleProcessQueue` 改為解析前重新 fetch `bank_account_mapping`，不再信任 stored `mapped_account_id`
- `SIGNED_IN` event 重置 `autoProcessFired.current = false`，re-login 後 auto-process 正確重觸發

## Scan-window hardening
- 新增 `gmail_last_successful_scan_at` 至 `user_settings`（migration 20260613000002）
- Cron 改為 per-user afterDate（`last_successful_scan_at - 2天` overlap），首次用 35 天
- Gmail list fetch 成功才推進 `gmail_last_successful_scan_at`，失敗不推進
- `job_runs.report` 記錄 `scan_windows`（after_date、messages_fetched、queued、scan_succeeded）

## Subrequest budget guard
- 建立 `job_runs` 表（production DB 之前缺失）
- `computeSafeMaxResults(userCount)` 動態計算安全 maxResults（budget=45，fixed=4/user，per_msg=2）
- `job_runs.report` 記錄 `subrequest_budget`、`requested_max_results`、`effective_max_results`

## Failed job retry path
- UI 加 `failedQueue` 狀態，fetch `failed` jobs on mount
- `failed` 和 `needs_review + parse_error` 顯示「重試」按鈕
- `needs_review + missing_mapping` 顯示「請先設定帳戶對應」提示
- PATCH retry 清 `error_code`、`error_message`、`review_reason`，job 進 `pendingQueue`，觸發 auto-process
- SettingsPanel `onMappingSaved` callback → App.tsx `gmailRefreshKey` → GmailSyncPanel queue refresh

## 永豐台股持倉 XLSX import
- 新增 `apps/api/src/lib/sinopac-holdings.ts`：解析永豐持倉 xlsx（商品、今餘、成本、現價）
- 新增 API route `POST /api/import/sinopac-holdings-xlsx`：upsert `holdings` + `price_snapshots`
- ImportPanel 加「永豐台股持倉 XLSX」選項（file accept xlsx）
- shared type `HoldingsImportResponse` 新增

## Bug 修正
- 月報 API `monthEnd` 固定用 `-31` 導致 6 月報表失敗（date out of range）→ 改用 `new Date(year, month, 0).getDate()` 算正確月底
- `PortfolioPanel` 因 `net_worth_snapshots` 表不存在而整個掛掉 → `netWorth`/`dividends` 改成 optional，只有 `holdings` 失敗才 bail out，其餘降級顯示

## 首頁佈局調整
- `PortfolioPanel` 從「進階檢視」移至「核心工作區」上方獨立區塊（`home-portfolio-workspace`）
- 持倉明細在匯入完成後立即可見，不需捲動到頁底

## Pending migrations（需手動 Supabase SQL Editor 執行）
- `ALTER TABLE import_jobs ADD COLUMN IF NOT EXISTS review_reason TEXT;`
- `ALTER TABLE user_settings ADD COLUMN IF NOT EXISTS gmail_last_successful_scan_at TIMESTAMPTZ;`
- `CREATE TABLE IF NOT EXISTS job_runs (...)` — 已執行
