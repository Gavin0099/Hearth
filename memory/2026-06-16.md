# 2026-06-16


- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Added a 15-second apiFetch timeout, made updateImportJob throw on non-2xx responses, and wrapped Gmail queue status writes in safeUpdateImportJob so a stuck or failed PATCH cannot block the pending queue. Bumped versions to 0.3.13.
  commit: 2cfe922
  commit_hash: 2cfe922
  session_id: cli-20260616-014456-gmail-api-timeout-safe-updates
  memory_binding: bound
  test_evidence: npm.cmd --workspace @hearth/web run check -> PASS; npm.cmd run check -> PASS.
  next_step: Deploy 0.3.13 and refresh/re-run the live Gmail queue; the first Mega job should no longer block queue advancement even if its status PATCH times out.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Fixed stale Gmail queue display after browser-side processing by adding per-session import job status overrides, applying imported/failed/needs_review statuses immediately, and merging those overrides into loadQueues so backend pending_parse lag does not restore stale 待匯入 badges. Bumped versions to 0.3.14.
  commit: 422ffdc
  commit_hash: 422ffdc
  session_id: cli-20260616-023955-gmail-queue-optimistic-refresh
  memory_binding: bound
  test_evidence: npm.cmd --workspace @hearth/web run check -> PASS; npm.cmd run check -> PASS.
  next_step: Deploy 0.3.14 and live-test Settings > Gmail 帳單同步: after 即時匯入 finishes, pending count should clear or shrink and the detected/processed list should show imported/failed/needs_review instead of stale 待匯入.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Fixed Gmail statement re-import duplicates by removing category from transaction source_hash and adding a Gmail natural-key compatibility check over existing account/date/source rows so old categorized Cathay transactions are skipped when the same statement is imported again. Bumped versions to 0.3.15.
  commit: 0117146
  commit_hash: 0117146
  session_id: cli-20260616-025141-gmail-reimport-dedupe
  memory_binding: bound
  test_evidence: npm.cmd --workspace @hearth/api run test -- tests/golden-portfolio-import.test.ts -> PASS 182/182; npm.cmd --workspace @hearth/api run test -- tests/auth-accounts.test.ts -> PASS 182/182; npm.cmd --workspace @hearth/api run check -> PASS.
  next_step: Deploy 0.3.15 and re-run Cathay Gmail import; already-imported rows should be skipped. Existing duplicate production rows created before this fix may need a separate cleanup.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Added scripts/gmail-transactions-dedupe-cleanup.ps1 to preview and optionally delete existing duplicate Gmail transaction rows by category-independent natural key. The script defaults to preview, scopes to source LIKE gmail_%, keeps categorized rows first, and requires UserEmail/UserId or explicit AllUsers for Apply. Bumped versions to 0.3.16.
  commit: 2f371eb
  commit_hash: 2f371eb
  session_id: cli-20260616-030006-gmail-duplicate-cleanup-script
  memory_binding: bound
  test_evidence: powershell -ExecutionPolicy Bypass -File scripts/gmail-transactions-dedupe-cleanup.ps1 -UserEmail reiko0099@gmail.com -PrintSqlOnly -> PASS; same command with -Apply -PrintSqlOnly -> PASS.
  next_step: Deploy 0.3.16, then run the cleanup script in preview mode against Supabase for reiko0099@gmail.com and review delete_ids before applying cleanup.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Fixed GitHub CI governance drift protected_files_unmodified failure by normalizing AGENTS.base.md and .governance/baseline.yaml to LF via .gitattributes and updating sha256.AGENTS.base.md to the LF checkout hash c16617acca72b517fe6f6280e75372a1b30ef7b10edd6ed9000a1a948ca691b9.
  commit: a39ef89
  commit_hash: a39ef89
  session_id: cli-20260616-031109-governance-protected-file-eol
  memory_binding: bound
  test_evidence: governance_drift_checker.py --repo . --framework-root ai-governance-framework --format human -> PASS severity=ok
  next_step: Push the governance protected-file EOL fix and confirm GitHub CI no longer reports AGENTS.base.md protected hash drift.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Fixed scripts/gmail-transactions-dedupe-cleanup.ps1 SQL user scoping after live preview failed with operator does not exist: uuid = text. UserEmail scope now compares accounts.user_id to auth.users.id as uuid, UserId is cast to uuid, and preview id aggregation casts uuid ids only for display.
  commit: 8d46448
  commit_hash: 8d46448
  session_id: cli-20260616-032249-gmail-dedupe-cleanup-uuid
  memory_binding: bound
  test_evidence: PrintSqlOnly preview PASS; PrintSqlOnly apply PASS; PowerShell parser check PASS.
  next_step: Pull the hotfix on the machine running cleanup, rerun preview for reiko0099@gmail.com, then review delete_ids before applying.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Added Gmail import mapping guards so explicit bank_account_mapping rows are ignored unless the mapped account type matches the Gmail source_type. This prevents stale credit_card->cash_bank or bank_account->cash_credit mappings from routing Gmail jobs into the wrong account, and the browser queue path now enforces the same rule.
  commit: 319d335
  commit_hash: 319d335
  session_id: cli-20260616-035534-gmail-invalid-mapping-guard
  memory_binding: bound
  test_evidence: npm.cmd --workspace @hearth/api run test -- tests/import-jobs.test.ts -> PASS 183/183; npm.cmd --workspace @hearth/api run check -> PASS; npm.cmd --workspace @hearth/web run check -> PASS.
  next_step: Deploy the invalid-mapping guard, inspect existing bank_account_mapping rows for reiko0099@gmail.com, then clean the already-misrouted Gmail transactions separately.

- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: Fixed the remaining manual Gmail credit-card import path in GmailSyncPanel so single-item imports now resolve through the credit-card-only auto-mapping helper instead of the generic bank matcher. This closes the wrong-account route that could write the same statement into a same-bank cash_bank account after the invalid-mapping guard landed.
  commit: fb16d35
  commit_hash: fb16d35
  session_id: cli-20260616-054251-gmail-manual-import-account-type
  memory_binding: bound
  test_evidence: npm.cmd --workspace @hearth/web run check -> PASS; governance_drift_checker.py --repo . --framework-root ai-governance-framework --format human -> PASS severity=ok.
  next_step: Deploy commit fb16d35, then live-test one manual Gmail credit-card import and verify it lands only in the credit-card account with no new cross-account duplicates.
