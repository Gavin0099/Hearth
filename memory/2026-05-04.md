# 2026-05-04

## Session Summary
- Updated `ai-governance-framework` submodule to latest `origin/main` (`a502b80`) and committed pointer update in Hearth (`61124b5`).
- Aligned credit-card import date semantics to `posted date` / `入帳起息日` across CSV and shared PDF parsers; pushed as `1aacc03`.
- Expanded `excel-monthly` parser for formula-heavy sheets:
  - parses formula-like amount cells without cached values
  - expands recurring/sidebar candidate capture for section-detail rows
  - pushed as `d6bb151`.
- Extended recurring-template pipeline to carry candidate amount from Excel sidebar candidates into `recurring_templates.amount`; pushed as `c0a8e66`.

## Key Decisions
- Credit-card monthly attribution now uses posted date as canonical transaction date for import consistency.
- Excel recurring candidates should carry optional amount metadata end-to-end to reduce post-import manual edits.

## Validation Notes
- `npm.cmd --workspace @hearth/api run build` passed after `excel-monthly` and recurring-candidate updates.
- `npm.cmd --workspace @hearth/web run check` passed after shared-type updates.
- In this sandbox, Node test runs still show `spawn EPERM`; functional assertions were updated in API tests and golden fixtures accordingly.

- Completed initial security-boundary consolidation and added docs/security-boundary.md with trust boundaries, enforced controls, known gaps, and hardening backlog.

- Added one-click recurring workflow in ImportPanel: create recurring templates from candidates and apply to current month in one action.
