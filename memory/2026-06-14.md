# 2026-06-14

## PortfolioPanel 精簡
- 移除「手動更新報價」整個區塊（表單 + CSV 上傳 + state + handlers）
- 只保留自動抓取（每日 14:00 cron）+ 刪除單筆報價功能

## InsurancePanel BENEFIT_MERGE_GROUPS
- `BENEFIT_MERGE_GROUPS` 定義需合併的給付標籤組
- 病房費/病房費補貼/住院醫療日額 → 合併為「病房費 / 住院日額」
- 意外身故/失能 系列也合併
- `sublabel` 欄位保留原始 label 在 row 內顯示（小字）
- `resolveGroupLabel()` 做映射

## InsurancePanel 正副本聚合視圖
- `BenefitItem` 加 `receiptType?: "正本" | "副本" | "日額"`
- `groupBenefitsByLabel()` 跨保單按 label 聚合，偵測正本+副本衝突
- 展開卡片改成「給付類型群組視圖」：每個 label 下列出所有保單的給付金額+申請方式
- 有衝突時顯示「先送正本後送副本」說明
- 無 benefits 資料時 fallback 到舊版 policy block 視圖
- receipt badge 顏色：正本=藍、副本=橘、日額=綠

## ParsedInsuranceRecord 擴充 benefits[]
- 新增 `BenefitItem = {label, amount, unit, note?}` 型別
- `ParsedInsuranceRecord` 加 `benefits?: BenefitItem[]` optional 欄位
- UI: 展開時若有 benefits 則顯示詳細給付表（日額/次限額/一次金）；否則 fallback 顯示主約保額
- SQL 已更新13張保單的benefits（富邦MHI日額2000/4000、全球HCB防癌日額5000、好心200月扶助20000等）

## InsurancePanel 保障卡展開明細
- 每個 coverage card 點擊後展開詳細表格（給付型態 / 保單名稱 / 保額）
- `getSubtypeLabel()` 依關鍵字推斷：初次罹癌一次金、終身累計限額、住院每次限額、急診每次限額、月給付型等
- 住院醫療 / 癌症防癌展開後顯示 `⚠ caveat` 說明不可直接加總
- 失能長照展開後顯示 3 級失能等級說明表格（第1–3/4–6/7–11級）
- `expandedCards: Set<CoverageCategory>` state，toggleCard() 切換

## InsurancePanel 保障總覽
- 新增 `buildCoverageSummary()` 函式，依關鍵字分類 13 張保單（壽險/傷害/住院/重大疾病/防癌/失能）
- `COVERAGE_CONFIG` 6 類，keywords RegExp 比對 productName
- 新增 `.coverage-summary-section` / `.coverage-summary-grid` / `.coverage-card--{key}` CSS
- 3 欄 grid，色帶左border，桌面 3×2 行，手機 2 欄

## Gmail sync cron bug 修正
- `index.ts` 的 cron 判斷條件從 `"0 2 5 * *"` 改為 `"0 2 * * *"`（之前永遠不 match → runGmailSync 從未執行）
- `computeAfterDate` 初次掃描從 35 天改為 180 天（6 個月）
- GmailSyncPanel browse UI 加入 import_jobs 狀態 flag（已匯入/排隊中/需設定帳戶/解析失敗）
- Badge 加入 error variant

## LoanPanel 餘額計算修正（重複計算問題）
- 同一帳號有多筆歷史記錄時，總計只取最新 paymentDate 的那一筆（`latestByAccount()`）
- `snapshotCount` 和最大餘額也改用 deduped 記錄計算
- 玉山資料清理 SQL：刪掉8筆，改存最新2筆（下方SQL）

## LoanPanel 攤還自動計算
- `ParsedLoanRecord` 加 optional `scheduleType?: "amortization"`, `monthlyPayment?`, `monthlyRate?`
- 存一筆「種子」記錄（參考日、餘額、月利率、月繳金額），UI 自動推算完整攤還表
- `computeAmortizationSchedule()` 從種子推算到清償（最多 480 期）
- 攤還種子從月份 tab 分離，獨立在「自動計算攤還」區段顯示
- 展開攤還表：已繳（dimmed）/ 本月（highlight）/ 未來（normal）三種樣式
- 永豐信貸種子：月利率 0.001809，月繳 38,567，參考日 2026-05-25，餘額 2,223,821

## 玉山房貸 SQL 插入
- `bank='esun'`, `type='loan'`, `statement_date='2026-06-01'`
- 帳號 0484165185406：9,600,000 寬限期（只繳息 17,440）
- 帳號 0484165185417：535,132 本息攤還（月繳 3,079）

## 首頁重構：總覽only
- Gmail 帳單同步、資料匯入、帳戶狀態 全部移至「設定」頁
- 首頁（總覽）只保留概覽卡片 + 月報表
- KPI 卡文案更新：指向設定頁、月報、投資組合
- 設定頁結構：Gmail同步 + 匯入 | 帳戶狀態 + 帳戶對應 | OpsPanel
