# 2026-03-31

- Expanded the shared transaction category list with bank-cashflow-specific labels after review of the bank ledger UX:
  - added `信用卡繳款`
  - added `質借借款`
  - added `質借還款`
- Expanded the same shared category list again for reconciliation-style offset items:
  - added `折讓款`
- Kept the additions in `packages/shared/src/transaction-categories.ts` so the same labels are available consistently across bank/credit-card review pickers and future rule-based auto-categorization.
- Improved review-list readability in both bank and credit-card ledgers:
  - transaction descriptions now allow two lines instead of a hard single-line ellipsis
  - full descriptions are also available via the native `title` hover text
- Added the missing `月支出趨勢` card to the credit-card review panel:
  - shows the recent 4 months as stacked monthly spend bars
  - uses bank color segments inside each month so the user can compare both total spend and bank mix at a glance
  - highlights the currently selected month in the chart
- Applied the same right-column trend language to the bank ledger:
  - added a `月資金流趨勢` card for the recent 4 months
  - uses bank-colored stacked bars based on monthly absolute cashflow volume
  - aligned bank month-tab ordering with the credit-card page (oldest → newest in the recent 4-month window)
- Added a first usable rule-based auto-categorization pass in the web app without waiting on a DB migration:
  - both bank and credit-card ledgers now learn a local per-user rule when the user manually assigns a category
  - rules are keyed by normalized description + income/expense direction + panel scope (`bank` / `credit`)
  - matching uncategorized transactions are auto-applied on later loads and after future manual classifications
  - rules are currently stored in browser `localStorage`, which keeps the feature immediately testable on the deployed app while leaving room for a future Supabase-backed rule table
- Verification: `npm --workspace @hearth/api run build`, `npm --workspace @hearth/web run check`.
- Added API worker cron wiring for daily market data refresh:
  - `apps/api/src/cron/daily-update.ts` now normalizes TWSE stock snapshot dates and FX rate dates before upserting `price_snapshots` / `fx_rates`
  - `apps/api/src/index.ts` explicitly exports the worker `scheduled` handler so the cron path stays visible at the entrypoint
  - `apps/api/wrangler.jsonc` now schedules a weekday `0 6 * * 1-5` trigger, which maps to Taiwan 14:00 after market close
- Synced local `ai-governance-framework` to `gavin0099/main` at `ef34729`, which now includes `docs/beta-gate/reviewer-run-sheet.md` alongside `reviewer-test-pack.md` and `reviewer-signal-split.md`.
- Captured the next Beta Gate action in `docs/beta-gate-reviewer-run-2026-03-31.md`:
  - do one fresh external cold-start reviewer onboarding run before adding more framework features
  - classify the first meaningful failure layer as discoverability / interpretation / decision reconstruction / escalation judgment
  - choose the smallest next fix only after score + override + signal split are recorded
- Added two repo-local execution aids for the next reviewer pass:
  - `docs/beta-gate-reviewer-handoff-2026-03-31.md` is now reduced to the exact repo URL + one-sentence framing allowed by the framework's cold-start rule
  - `docs/beta-gate-reviewer-record-2026-03-31.md` is the author-side scoring and failure-layer capture sheet
- Closed the missing read path for dividend imports in Hearth itself:
  - added `GET /api/portfolio/dividends` so imported `dividends` rows are finally user-visible through the product, not just stored in Supabase
  - extended `PortfolioPanel` to show recent dividend payouts alongside holdings / FX / net worth
  - added shared dividend response types so API and web stay aligned on the new slice
- Verification for the dividend visibility slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` is blocked in sandbox with `spawn EPERM`
  - `npm.cmd --workspace @hearth/web run check` still fails on pre-existing `tesseract.js` module/type issues in `apps/web/src/lib/pdf-parser.ts`
- Extended the portfolio slice so dividend data is visible in the investment summary without corrupting net worth:
  - `GET /api/portfolio/net-worth` now also returns `dividendsReceivedTwd` and `dividendsYearToDateTwd`
  - `PortfolioPanel` shows those dividend summary fields alongside cash / investments, but `totalNetWorthTwd` still only reflects cash + marked-to-market holdings
- Added the first repo-level foreign brokerage import path instead of keeping Phase E blocked on TW-only terminology:
  - `POST /api/import/foreign-stock-csv` reuses the existing stock-trade parser / holdings recalculation path but preserves non-TWD trade currency
  - `ImportPanel` now exposes a dedicated `複委託交易 CSV` mode so `investment_foreign` accounts have a product-visible entry point
- Pulled the API verification surface back to stable:
  - updated import-route tests to match the route's `transactions.upsert` contract
  - added coverage for FX-adjusted net worth + dividend summary and for `foreign-stock-csv`
  - fixed two parser regressions in `packages/shared/src/pdf-credit-card.ts` so bank sub-accounts stay optional and fragmented 玉山 loan sections recover both balance and payment date correctly
- Verification for the portfolio/import stabilization slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`51/51`)
  - `npm.cmd --workspace @hearth/web run check` was not rerun because the workspace is still blocked by the pre-existing `tesseract.js` module/type issue in `apps/web/src/lib/pdf-parser.ts`
- Added another `P0 correctness` pass instead of expanding feature surface:
  - `GET /api/report/monthly` now has explicit regression coverage for month-boundary exclusion and same-day aggregation across multiple owned accounts
  - `POST /api/import/dividends-csv` now has duplicate/source-hash coverage so imported dividend rows are locked to idempotent behavior
  - the API test suite now captures both transaction import idempotency and dividend import idempotency as first-class product guarantees, not just route availability
- Verification for the correctness pass:
  - `npm.cmd --workspace @hearth/api run test` passed (`53/53`)
- Started the first `P0 security` containment slice on secrets exposure:
  - `GET /api/user-settings` no longer returns raw PDF passwords; it now returns only `has_*` presence flags plus Gmail metadata
  - added explicit `GET /api/user-settings/pdf-passwords` for the narrow Gmail-sync use case that still needs the raw secrets client-side today
  - added `Cache-Control: no-store` on both settings endpoints so password-bearing or password-adjacent responses are not cacheable by default
  - `SettingsPanel` is now write-only for PDF passwords and supports explicit clear actions without round-tripping existing secrets back into the UI
- This slice intentionally does **not** claim the password problem is solved:
  - `supabase/schema.sql` still stores these password fields as plaintext columns
  - proper encryption, key management, rotation, and deletion strategy remain open `P0 security` work
- Verification for the secrets-exposure containment slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`55/55`)
  - `npm.cmd --workspace @hearth/web run check` still fails only on the pre-existing `tesseract.js` module/type issue in `apps/web/src/lib/pdf-parser.ts`
- Extended the same `P0 security` slice from exposure reduction into app-layer encryption at rest:
  - added `apps/api/src/lib/secrets.ts` with PBKDF2 + AES-GCM helpers keyed by `USER_SETTINGS_SECRET_KEY`
  - `PUT /api/user-settings` now encrypts PDF password fields before persisting them to `user_settings`
  - `GET /api/user-settings/pdf-passwords` now decrypts those fields server-side before returning them to the narrow Gmail-sync client path
  - both read and write paths now fail cleanly with a 500 when `USER_SETTINGS_SECRET_KEY` is not configured, instead of partially proceeding
- This encryption slice is intentionally backward-compatible rather than a full migration:
  - previously stored plaintext password rows still read successfully via a non-versioned fallback path
  - existing Supabase rows are not yet automatically re-encrypted or rotated
  - `apps/api/wrangler.jsonc` now documents the required `wrangler secret put USER_SETTINGS_SECRET_KEY` setup step
- Verification for the app-layer encryption slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`56/56`)
  - `npm.cmd --workspace @hearth/web run check` still fails only on the pre-existing `tesseract.js` module/type issue in `apps/web/src/lib/pdf-parser.ts`
