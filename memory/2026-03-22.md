# 2026-03-22

- Continued the recurring-template track after yesterday's baseline adoption milestone.
- Extended the recurring template model so templates are now tied to a target account:
  - `recurring_templates.account_id` added to `supabase/schema.sql`
  - shared recurring types now include `account_id`
- Extended recurring template creation flows:
  - manual template creation now requires an owned account
  - Excel recurring candidates now bulk-create templates for the selected import account
- Added the first recurring apply flow:
  - Worker now exposes `POST /api/recurring-templates/apply`
  - applying templates writes monthly rows into `transactions`
  - duplicate month/template combinations are skipped through `source_hash`
- Updated frontend wiring so recurring templates can participate in the existing cashflow UI:
  - recurring template panel now loads owned accounts
  - recurring template panel now has an action to apply the current month's templates
  - transaction history panel can refresh after recurring apply
  - monthly report refreshes through the same shared app refresh path
- Added the first transaction correction flow:
  - Worker now exposes `DELETE /api/transactions/:transactionId`
  - recent transactions in the UI can now be deleted directly
  - deletion is scoped to the current user's owned accounts
- Added the first transaction lookup flow:
  - `GET /api/transactions` now supports account/category/date/keyword filters
  - transactions panel now has filter controls for account/date range/category/keyword
  - this expands transaction usability beyond a fixed recent list
- Added the first transaction edit flow:
  - Worker now exposes `PUT /api/transactions/:transactionId`
  - recent transactions in the UI now support inline edit and save
  - update path is scoped to the current user's owned accounts
- Added first-release readiness automation:
  - added `scripts/first-release-readiness.ps1`
  - added npm commands:
    - `readiness:first:codeonly` (skip env + web build)
    - `readiness:first` (skip web build)
    - `readiness:first:strict` (full check)
  - documented flow in `docs/first-release-readiness.md`
- Verification:
  - `npm --workspace @hearth/api run test` passed
  - result: `25/25` tests passed
  - `npm --workspace @hearth/api run build` passed
  - `npm --workspace @hearth/web run check` passed
  - `npm run readiness:first` currently fails in this workspace until `.env` and `apps/api/.dev.vars` are configured
  - `npm run readiness:first:codeonly` passed
  - after env updates, `npm run readiness:first` passed
  - `npm run readiness:first:strict` passed
- Readiness hardening follow-up:
  - adjusted readiness env rule so local `VITE_API_BASE_URL` is accepted for dev
  - changed web build command to `tsc --noEmit && vite build` to prevent generated `.js` files in source folders
- Added first Cloudflare deploy automation:
  - added `scripts/cloudflare-first-deploy.ps1`
  - added npm commands:
    - `deploy:first:dry`
    - `deploy:first`
  - documented flow in `docs/cloudflare-first-deploy.md`
- Ran real deploy attempt and captured blockers:
  - Worker deploy initially failed on `node:crypto` import without `nodejs_compat`
  - Pages deploy initially failed because project `hearth-web` did not exist yet
- Applied deploy hardening:
  - added `nodejs_compat` to `apps/api/wrangler.jsonc`
  - upgraded deploy script to fail-fast on command errors (`LASTEXITCODE`)
  - upgraded deploy script to auto-create Pages project when missing
  - removed unsupported `--commit-dirty` flags for this Wrangler version
- Completed first real Cloudflare deployment:
  - Worker deployed: `https://hearth-api.reiko0099.workers.dev`
  - Pages deployed: `https://ad4d7cc8.hearth-web.pages.dev`
- Added post-deploy smoke-test automation:
  - added `scripts/post-deploy-smoke.ps1`
  - added npm command: `smoke:postdeploy`
  - documented flow in `docs/post-deploy-smoke.md`
  - verifies API `/health` and Pages web root availability
  - executed against live endpoints and passed
- Added repo-specific engineering governance baseline:
  - added `governance/AGENT.md` for L0/L1/L2 classification and execution rigor
  - added `governance/ARCHITECTURE.md` for app/api/db boundaries and invariants
  - added `governance/TESTING.md` for verification gates
  - added `governance/rules/common/core.md` for Hearth-specific rule pack
  - updated `contract.yaml` with real repo/domain values and governance documents
  - updated root `AGENTS.md` to reference local governance as active canonical source
- Expanded post-deploy smoke to optional authenticated mode:
  - `scripts/post-deploy-smoke.ps1` now supports `-BearerToken`
  - when token is provided, smoke also verifies:
    - `GET /api/auth/me`
    - `GET /api/accounts`
  - non-auth smoke path remains passing against live production endpoints
- Expanded post-deploy smoke to optional transaction probe mode:
  - supports `-AccountId` + `-ExerciseTransactions`
  - performs create -> query -> delete flow against live `/api/transactions`
  - uses unique probe description and `finally` cleanup to avoid data residue
- Added first credit-card import entrypoint:
  - added `POST /api/import/credit-card-tw`
  - added parser `apps/api/src/lib/credit-card.ts`
  - connected frontend import mode `Credit Card TW CSV`
  - added API test coverage for minimal credit-card csv mapping
- Expanded post-deploy smoke again:
  - added `-ExerciseReport` option to verify `/api/report/monthly`
  - `-ExerciseTransactions` now auto-resolves first owned account when `-AccountId` is omitted
  - keeps transaction probe auto-cleanup behavior
- Expanded post-deploy smoke for import/recurring route checks:
  - added `-ExerciseImports` for safe import endpoint validation checks (no file, expect validation errors)
  - added `-ExerciseRecurring` for recurring list and apply-validation checks
  - these probes focus on auth + route wiring and avoid persistent data writes
- Added governance phase-gate automation:
  - added `scripts/governance-phase-gate.ps1`
  - added `npm run governance:gate`
  - added docs `docs/governance-phase-gate.md`
  - checks local governance files, PLAN freshness, and today's memory file presence
- Integrated governance gate into release-readiness flow:
  - `scripts/first-release-readiness.ps1` now runs governance gate by default
  - `readiness:first` and `readiness:first:strict` include governance gate
  - `readiness:first:codeonly` keeps fast path by skipping governance gate
- Added first real portfolio holdings slice:
  - `GET /api/portfolio/holdings` now reads Supabase `holdings` table with owned-account scope
  - added web `PortfolioPanel` to show holdings list in dashboard
  - added API test coverage for unauthorized and owner-scoped success path
- Fixed production API base URL wiring for deployed web:
  - `cloudflare-first-deploy.ps1` now injects `VITE_API_BASE_URL` during web build
  - added production fallback in `apps/web/src/env.ts` for `.pages.dev` host
  - this prevents deployed UI from pointing at `http://127.0.0.1:8787`
- Current product state:
  - auth/accounts flow is stable
  - monthly report reads real transactions
  - transactions can come from manual entry, normalized CSV, Sinopac CSV, Excel monthly import, and recurring-template apply
  - transactions now support add/filter/edit/delete for daily correction loops
  - recurring templates are no longer only configuration records; they now feed the main cashflow pipeline
