# 2026-04-01

- Continued `P0 security` on `user_settings` instead of expanding new product surface:
  - kept the client contract unchanged (`GET /api/user-settings` for presence flags, `GET /api/user-settings/pdf-passwords` for the narrow secret path)
  - added a legacy-rotation path so old plaintext PDF passwords do not stay in the table indefinitely after the new encryption rollout
- `GET /api/user-settings/pdf-passwords` now performs opportunistic upgrade-on-read:
  - if a stored password field is already `v1.*` ciphertext, it is only decrypted for the response
  - if a stored password field is legacy plaintext, the Worker now encrypts it with `USER_SETTINGS_SECRET_KEY` and upserts the ciphertext back into `user_settings`
  - the response still returns the raw password string to the caller so the Gmail sync flow remains unchanged
- This keeps the rollout backward-compatible while shrinking the plaintext tail:
  - no explicit admin migration command is required for active users
  - rows that are never read through the secret path may still remain plaintext until a later dedicated migration/rotation pass
- Added regression coverage in `apps/api/tests/auth-accounts.test.ts` for the upgrade-on-read behavior so legacy plaintext rows are re-written as ciphertext after the first explicit secret fetch.
- Started `P0 schema discipline` so database evolution has history instead of a single mutable baseline:
  - added `supabase/migrations/20260401000000_baseline.sql` as the first committed schema-history entry
  - documented `supabase/schema.sql` as the fresh-bootstrap snapshot and `supabase/migrations/` as canonical change history
  - updated repo governance/docs so future schema changes must update both the migration history and the latest bootstrap snapshot
- Added the missing active backfill path for `user_settings` secrets:
  - extracted shared legacy-secret upgrade logic into `apps/api/src/lib/secrets.ts`
  - added `apps/api/scripts/backfill-user-settings-secrets.ts` with dry-run default and explicit `--write` mode
  - the script scans `user_settings`, rewrites only legacy plaintext password fields, and leaves existing `v1.*` ciphertext untouched
  - documented the script in `README.md` so the remaining plaintext tail can be rotated without relying on user traffic
- Verification for the backfill-tooling slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`59/59`)
  - `npm.cmd --workspace @hearth/api run backfill:user-settings-secrets` starts correctly and fails fast on missing env (`SUPABASE_URL is required.`), which confirms the script entrypoint is wired without running a real backfill
- Pulled `P0 correctness` forward on import idempotency, not just coverage count:
  - fixed batch-local dedupe in `apps/api/src/routes/import.ts` so repeated rows inside the same upload no longer inflate `imported` counts or rely on DB conflict behavior to clean up
  - applied the same rule to `transactions-csv`, `foreign-stock-csv` / investment trades, and `dividends-csv`
  - added regression tests for duplicate rows within a single upload batch so `imported/skipped` stays truthful for cashflow, dividend, and brokerage imports
- Verification for the batch-dedupe correctness slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`62/62`)
- Added the first fixture-driven correctness matrix for report/import behavior instead of only route-level spot checks:
  - exported the pure monthly-report summarizer from `apps/api/src/routes/report.ts`
  - added `apps/api/tests/golden-import-report.test.ts` with golden cases for monthly summary aggregation and `excel-monthly` workbook parsing
  - the new matrix locks category fallback, same-day aggregation, date inference from sheet names, merged-cell handling, and pair-column/grid workbook shapes
- Verification for the first golden-matrix slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`66/66`)
- Extended the same fixture-driven correctness approach to CSV parser contracts:
  - added `apps/api/tests/golden-parser-import.test.ts`
  - locked representative `sinopac-tw` and `credit-card-tw` CSV cases around date parsing, debit/credit sign handling, refund positivity, payment skipping, and validation error retention
  - used the parser-supported English header aliases in fixtures so the matrix checks real parser behavior rather than repo encoding artifacts
- Verification for the parser-fixture slice:
  - `npm.cmd --workspace @hearth/api run test` passed (`70/70`)
- Continued `P0 correctness` on the portfolio import surface rather than adding more routes:
  - added `apps/api/src/lib/dividends.ts` as a pure dividends CSV normalization helper with stable validation and source-hash generation
  - added `apps/api/tests/golden-portfolio-import.test.ts` to lock `dividends-csv` and `sinopac-stock` normalization behavior through fixture-driven cases
  - the new matrix covers ticker/date/amount normalization, default currency, stable source hashes, ROC stock-date parsing, buy/sell action mapping, and retention of valid rows alongside row-level validation failures
  - intentionally left `apps/api/src/routes/import.ts` runtime wiring unchanged for now because the current dividends import block has encoding-corrupted text and is risky to refactor blindly in the same slice
- Verification for the portfolio-import fixture slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`74/74`)
- Closed the gap between the new dividends helper and the runtime import path:
  - wired `POST /api/import/dividends-csv` to reuse `apps/api/src/lib/dividends.ts` normalization results instead of letting the route drift from the tested parser contract
  - added route-level regression coverage that proves mixed valid/invalid dividend rows still import the valid row while surfacing stable validation errors
  - kept the old inline parsing block in place for now because that part of `apps/api/src/routes/import.ts` still contains encoding-corrupted text; the route behavior now follows the shared helper, but the dead legacy block should be cleaned up in a later focused refactor
- Verification for the runtime-wiring slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`75/75`)
- Extended the same runtime-level correctness coverage to foreign brokerage imports:
  - added a `POST /api/import/foreign-stock-csv` regression that exercises mixed invalid/valid rows through the real route, not just the pure stock parser
  - locked the partial-success contract at the route boundary: valid trades still persist, holdings still recalculate, and row-level parser failures still surface in the response payload
  - had to align the test doubles with the route's real Supabase usage (`resolveOwnedAccountIds`, `investment_trades.upsert`, and the chained holdings-rebuild query), which also exposed the actual persistence contract more clearly
- Verification for the foreign-stock runtime regression slice:
  - `npm.cmd --workspace @hearth/api run test` passed (`76/76`)
- Refactored the foreign-stock holdings rebuild rule into a pure helper instead of leaving weighted-cost math embedded inside the route:
  - added `apps/api/src/lib/holdings.ts` with `rebuildHoldingFromTrades`
  - `POST /api/import/foreign-stock-csv` now delegates the weighted-average / share-reduction / zero-position collapse logic to that helper before upserting or deleting `holdings`
  - added pure golden tests for three contracts: weighted buy average, sell without average-cost reset, and full liquidation -> `null`
- Verification for the holdings-helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`79/79`)
- Pulled the remaining dividend-import batch logic into a pure helper instead of leaving DB-diff semantics embedded inside the route:
  - added `prepareDividendImportBatch` to `apps/api/src/lib/dividends.ts`
  - `POST /api/import/dividends-csv` now reuses the helper for payload dedupe, existing-hash filtering, and `skipped` counting
  - added golden tests for duplicate-in-payload plus already-existing-hash behavior so the import summary contract is locked outside the route
- Verification for the dividend-batch-helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`81/81`)
- Matched the same refactor pattern on foreign brokerage imports so stock-import summary rules are no longer route-only logic:
  - added `prepareStockTradeImportBatch` to `apps/api/src/lib/sinopac-stock.ts`
  - `POST /api/import/foreign-stock-csv` now reuses that helper for payload dedupe, existing-hash filtering, and `skipped` calculation
  - added pure golden cases for duplicate-in-payload plus already-existing-hash behavior, keeping stock import batch semantics aligned with dividends
- Verification for the stock-batch-helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`83/83`)
- Continued the same route-thinning pattern for foreign brokerage imports:
  - added `prepareStockTradeImportBatch` to `apps/api/src/lib/sinopac-stock.ts`
  - `POST /api/import/foreign-stock-csv` now delegates payload dedupe, existing-hash filtering, and `skipped` counting to the shared helper
  - added pure golden cases for stock import batch semantics so the route no longer owns that contract by itself
- Verification for the stock-import batch refactor slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`83/83`)
- Finished aligning the third import path with the same batch-helper pattern:
  - added `apps/api/src/lib/transaction-import.ts` for transaction source-hash expansion plus payload/existing-row diffing
  - `importNormalizedRows` in `apps/api/src/routes/import.ts` now delegates transaction batch semantics to the helper instead of carrying duplicate/skip math inline
  - added pure golden cases for duplicate-in-payload and existing-hash filtering so `transactions-csv` batch behavior is locked the same way as dividends and stock imports
- Verification for the transaction-batch-helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`85/85`)
- Completed the same batch-helper refactor across the cashflow import path:
  - added `apps/api/src/lib/transaction-import.ts` so transaction hash expansion and payload/existing-row diffing are no longer route-only logic
  - `importNormalizedRows` in `apps/api/src/routes/import.ts` now delegates transaction batch preparation to that helper
  - added pure golden cases for duplicate-in-payload and existing-hash filtering so all three import families now share the same style of batch-contract coverage
- Verification for the transaction-import helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`85/85`)
- Completed the repo-local governance and memory adoption layer so Hearth no longer depends on the framework submodule alone for runtime instructions:
  - added local `governance/SYSTEM_PROMPT.md`, `governance/HUMAN-OVERSIGHT.md`, and `governance/REVIEW_CRITERIA.md`
  - added structured memory files: `memory/01_active_task.md`, `memory/02_project_facts.md`, `memory/03_knowledge_base.md`, `memory/03_decisions.md`, `memory/04_validation_log.md`
  - updated `AGENTS.md`, `MEMORY.md`, and `contract.yaml` so the repo explicitly declares the local governance + structured-memory workflow
- Verification for the governance-adoption slice:
  - `npm.cmd run readiness:first:codeonly` completed
  - readiness still shows the known sandbox `spawn EPERM` issue when the script invokes API tests through nested PowerShell/cmd execution
  - readiness still shows the pre-existing `tesseract.js` type/module failure in `apps/web/src/lib/pdf-parser.ts`
- Ran a separate scratch-clone validation of framework memory-schema behavior against a pre-adoption Hearth state:
  - used `E:\BackUp\Git_EE\hearth-memory-check` at commit `03dca60` after cleaning untracked scaffold files
  - pre-adoption results correctly reported `memory_schema_status=partial` and missing logical names `active_task`, `knowledge_base`, `review_log`
  - post-adoption results correctly created canonical scaffold files and moved readiness/intake to `schema_status=complete`
  - recorded the full run in `docs/hearth-memory-schema-run-2026-04-01.md`
- Returned focus to `Hearth` product work and cleaned a real import-route debt instead of staying on framework validation:
  - removed the legacy inline `dividends-csv` parsing block from `apps/api/src/routes/import.ts`
  - the route now uses only `parseDividendsCsv` + `prepareDividendImportBatch`, matching the existing golden tests and route-level regression coverage
  - switched the dividend route ownership check to the shared `resolveOwnedAccountIds` path so account authorization semantics now match the other import endpoints
- Fixed two pre-existing syntax-broken fallback error strings in `import.ts` while stabilizing the file:
  - the malformed `sinopac-stock` fallback error string now reads `CSV rows are invalid.`
  - the malformed `foreign-stock-csv` fallback error string now reads `Foreign stock CSV rows are invalid.`
- Updated the three dividend import route tests in `apps/api/tests/auth-accounts.test.ts` so the account stub matches the real `resolveOwnedAccountIds` contract (`select(...).eq(...) -> { data: [...] }`) instead of the older `maybeSingle()` shape.
- Verification for the dividend-route cleanup slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`85/85`)
- Continued thinning `apps/api/src/routes/import.ts` instead of adding new product surface:
  - removed the last route-local `dedupeBySourceHash` usage from `sinopac-stock`
  - switched `sinopac-stock` to the same `prepareStockTradeImportBatch` helper already used by `foreign-stock-csv`
  - replaced the route-local weighted-cost / liquidation math in `sinopac-stock` with the shared `rebuildHoldingFromTrades` helper, so both stock import routes now share the same holdings semantics
- Verification for the `sinopac-stock` helper-alignment slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`85/85`)
- Continued shrinking stock import orchestration instead of leaving the last shared DB flow duplicated in two routes:
  - added `normalizeHoldingPersistenceTrades` and `refreshHoldingsForTickers` to `apps/api/src/lib/holdings.ts`
  - both `sinopac-stock` and `foreign-stock-csv` now delegate holdings refresh persistence to that shared helper
  - added helper-level golden coverage for persistence-trade normalization and the open-position vs fully-closed-position refresh path
- Verification for the holdings-refresh extraction slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`87/87`)
- Continued route thinning on the stock import side so the route no longer owns the DB orchestration at all:
  - added `apps/api/src/lib/stock-import.ts`
  - moved existing-hash lookup, fresh-trade upsert, holdings refresh, and stock import response shaping into `executeStockTradeImport`
  - `sinopac-stock` and `foreign-stock-csv` now differ mainly in source labeling and the parse step that prepares `trades`
- Verification for the stock-import execution-helper slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`87/87`)
