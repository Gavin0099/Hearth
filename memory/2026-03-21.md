# 2026-03-21

- Created the initial `Hearth` monorepo scaffold with:
  - `apps/web`
  - `apps/api`
  - `packages/shared`
  - `supabase/schema.sql`
- Adopted `ai-governance-framework` as a submodule under `D:\Hearth\ai-governance-framework`
- Confirmed local `ai-governance-framework` repo was already at the latest `origin/main` commit when imported
- Switched the product architecture to:
  - Supabase for database, auth, and storage
  - Cloudflare Pages and Workers for deployment and execution
- Converted the API shell to Cloudflare Worker-compatible Hono structure
- Added the first real Supabase-backed route family:
  - `GET /api/accounts`
  - `POST /api/accounts`
- Upgraded the accounts API from temporary header-based user scoping to Supabase bearer-token user resolution
- Added initial frontend scaffolding for:
  - API environment wiring
  - Supabase browser client
  - accounts status panel
- Detected a governance gap:
  - `Hearth` had imported the framework submodule, but had not yet adopted the framework-style local plan and memory workflow
- Resolved that gap by creating:
  - `AGENTS.md`
  - `PLAN.md`
  - `MEMORY.md`
  - `memory/2026-03-21.md`
- Reviewed external product and repo references relevant to `Hearth`:
  - `Actual Budget`
  - `Firefly III`
  - `rotki`
  - `thounny/finance-tracker`
- Recorded that reference analysis in `docs/competitive-analysis.md`
- Main takeaways from the reference pass:
  - `Actual Budget` is the strongest reference for privacy, sync trust, and multi-device confidence
  - `Firefly III` is the strongest reference for finance-domain completeness
  - `rotki` is the strongest reference for future portfolio and net-worth depth
  - `thounny/finance-tracker` is the closest implementation-stage reference for current API and feature slicing
- Completed the next Phase B auth milestone:
  - added frontend Supabase session handling
  - added Google sign-in / sign-out controls
  - added Worker-side user verification display through `/api/auth/me`
  - made the accounts panel load only after a real authenticated session exists
- Added `docs/auth-setup.md` to capture the current auth flow and required Supabase configuration
- Completed the first frontend account write milestone:
  - added account creation form UI
  - wired it to `POST /api/accounts`
  - inserted the created account back into the on-screen list without waiting for a full reload
- Added local executable verification for the current core slice:
  - extracted a testable Worker app factory
  - added Node-based route tests for `/api/auth/me` and `/api/accounts`
  - documented the verification entrypoint in `docs/local-verification.md`
- Verification:
  - `npm --workspace @hearth/api run test` passed
  - result: `5/5` tests passed
- Added `docs/framework-collaboration-analysis.md` to capture both the benefits and the friction of applying `ai-governance-framework` to `Hearth`
- Started the first Phase C monthly-report slice:
  - added shared transaction and monthly report types
  - upgraded `GET /api/report/monthly` from stub to a Supabase-backed summary route
  - added a frontend monthly report panel
  - added a local API test for monthly report aggregation
- Added `docs/monthly-report-slice.md`
- Added the first manual transaction path:
  - `GET /api/transactions`
  - `POST /api/transactions`
  - frontend manual transaction panel
  - monthly report refresh trigger after transaction creation
- Added `docs/manual-transaction-slice.md`
- Added the first normalized CSV import path:
  - `POST /api/import/transactions-csv`
  - CSV parser utility for normalized transaction rows
  - frontend CSV import panel
  - local API test coverage for the import route
- Added `docs/csv-import-slice.md`
- Added the first Sinopac-specific import layer:
  - upgraded `POST /api/import/sinopac-tw` from stub to a minimal parser-backed mapping flow
  - added keyword-based category inference for the first mapped rows
  - added local API test coverage for the `sinopac-tw` path
- Added `docs/sinopac-import-slice.md`
- Added the first `excel-monthly` workbook import slice:
  - upgraded `POST /api/import/excel-monthly` from stub to a parser-backed workbook flow
  - added a constrained first-pass Excel layout using `分類` / `項目` plus date columns
  - reused the shared transaction insertion and dedupe path
- Added `docs/excel-monthly-slice.md`
- Updated repository Git identity to:
  - `Gavin0099`
  - `reiko0099@gmail.com`
- Pushed the initial `Hearth` milestones to `origin/main`
- Verification refresh:
  - `npm --workspace @hearth/api run test` passed
  - result: `11/11` tests passed
  - `npm --workspace @hearth/api run build` passed
  - `npm --workspace @hearth/web run check` passed
  - `npm --workspace @hearth/web run build` hit local `spawn EPERM` while Vite/esbuild tried to start in this environment
