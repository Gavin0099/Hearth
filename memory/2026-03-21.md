# 2026-03-21

- Created the initial `Hearth` monorepo scaffold with:
  - `apps/web`
  - `apps/api`
  - `packages/shared`
  - `supabase/schema.sql`
- Adopted `ai-governance-framework` as a submodule under `D:\Hearth\ai-governance-framework`
- Confirmed local `ai-governance-framework` repo was already at the latest `origin/main` commit when imported
- Switched the product architecture to:
  - Supabase for database, auth, and storage
  - Cloudflare Pages and Workers for deployment and execution
- Converted the API shell to Cloudflare Worker-compatible Hono structure
- Added the first real Supabase-backed route family:
  - `GET /api/accounts`
  - `POST /api/accounts`
- Upgraded the accounts API from temporary header-based user scoping to Supabase bearer-token user resolution
- Added initial frontend scaffolding for:
  - API environment wiring
  - Supabase browser client
  - accounts status panel
- Detected a governance gap:
  - `Hearth` had imported the framework submodule, but had not yet adopted the framework-style local plan and memory workflow
- Resolved that gap by creating:
  - `AGENTS.md`
  - `PLAN.md`
  - `MEMORY.md`
  - `memory/2026-03-21.md`
- Important current limitation:
  - Git commit/push workflow is blocked until this repo has explicit `user.name` and `user.email` configured
