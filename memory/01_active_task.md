# Current Task: Governance Sync Cleanup And Product Stabilization

## Current State

- Hearth `main` is synced with `origin/main`.
- AI Governance submodule is updated to `9b0e6b7ebff2d085861fade1054e773eaa630df1`.
- Governance drift currently passes with `severity=ok`.
- `version_compatibility.json` has been refreshed back to `compatible`.
- Claude, GitHub Copilot, and Gemini closeout hooks now point to Hearth's repo-local `ai-governance-framework` submodule and have smoke-tested compliant.

## Active Cleanup

- Remaining dirty tree is mostly runtime/session-end evidence generated by smoke tests and older closeout promotions.
- Do not bulk commit runtime artifacts without a scoped evidence policy.
- Keep `.codex/hooks.json` uncommitted until the framework manager verifies Codex as a real native hook surface rather than Tier B/stub.
- `stash@{0}` (`codex-pre-pull-tracked-20260611`) remains as a backup of pre-pull tracked dirty changes.

## Next

- Decide whether tracked runtime ledgers under `artifacts/runtime/*.ndjson` should be promoted, regenerated, or reset to committed state.
- Keep product work focused on the existing PLAN priorities: Gmail server-sync manual deployment validation, UI component restyling, and Security Phase F-1.
