# 2026-04-02

- Reviewed the current A1/B1/A2/C1/C2/D1/E1 TODO batch against the working tree instead of assuming they were all still open.
- Current status after inspection:
  - `A1+B1` look implemented in `TransactionsPanel` through month navigation plus incremental pagination / load-more.
  - `A2` looks implemented in `MonthlyReportPanel` through category click drill-down into filtered transactions.
  - `C1` was partially closed: API route, front-end chart, and snapshot migration existed, but regression coverage was missing.
  - `C2` was partially closed: route, shared types, and front-end table existed; API coverage and stricter response typing were the missing pieces.
  - `D1` is only partially done right now: `ImportPanel` has a client-side file preview, but not a true parser-backed dry-run preview contract.
  - `E1` had already been materially addressed in earlier slices through encrypted `user_settings`, upgrade-on-read, and the `backfill:user-settings-secrets` path.
  - `Final code review` was still open.
- Closed the immediate `C1/C2` verification gap:
  - aligned `trade-costs` success payload typing to `InvestmentCostsResponse`
  - added regression coverage proving `GET /api/portfolio/net-worth` writes the expected `net_worth_snapshots` row
  - added regression coverage for `GET /api/portfolio/net-worth-history`
  - added regression coverage for `GET /api/portfolio/trade-costs`
  - updated the holdings test stub so it matches the current holdings-price enrichment behavior
- Verification for the portfolio analytics coverage slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`96/96`)
- Started and closed `D1` as a real parser-backed slice instead of a fake file preview:
  - added shared `ImportPreviewResponse` types
  - added `POST /api/import/preview`
  - wired preview parsing through the same parser/normalization logic used by real imports
  - added API tests for normalized CSV preview and `excel-monthly` preview
  - updated `ImportPanel` to call the preview API and render parser-backed counts/errors/sample rows
- Tightened the `ImportPanel` preview failure path so thrown request failures clear preview state and do not leave the spinner stuck.
- Verification for the dry-run preview slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`98/98`)
- `npm.cmd --workspace @hearth/web run build` still fails only on the pre-existing `apps/web/src/lib/pdf-parser.ts` `tesseract.js` module/type issue; the new preview work did not introduce a new web build blocker.
- Continued from the final review findings instead of opening a new feature slice:
  - changed `trade-costs` to group by `ticker + currency` so USD/TWD fees are no longer mixed
  - updated the portfolio UI to display per-row currency and per-currency totals instead of a fake cross-currency grand total
  - changed portfolio loading order so `net-worth-history` is fetched after `fetchNetWorth()` writes the latest snapshot
  - added explicit monthly-report category drill-down error handling instead of silent empty expansion on fetch failure
- Verification for the review-fix slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`98/98`)
  - `npm.cmd --workspace @hearth/web run build` still only fails on the pre-existing `pdf-parser.ts` `tesseract.js` type/module issue
- Closed the remaining front-end type blocker in `pdf-parser.ts` by adding a local `apps/web/src/tesseract.d.ts` shim for the `tesseract.js` API that Hearth actually uses.
- Verification for the front-end typecheck fix:
  - `npm.cmd --workspace @hearth/web run check` passed
  - full `npm.cmd --workspace @hearth/web run build` inside the sandbox still fails later at Vite/esbuild config bundling with `spawn EPERM`, so that failure is now an execution-environment issue rather than a source-level TypeScript issue
- Continued on the next remaining web-productization gap after restoring buildability:
  - moved `GmailSyncPanel` off eager OCR imports and switched `../lib/pdf-parser` to runtime lazy loading
  - kept the OCR path behavior the same, but pushed `pdfjs-dist` + `tesseract.js` into a separate `pdf-parser` chunk
- Verification for the bundle-splitting slice:
  - `npm.cmd --workspace @hearth/web run check` passed
  - `npm.cmd --workspace @hearth/web run build` passed
  - main web bundle dropped from roughly `841 kB` to `349 kB` after minification, with OCR isolated into `assets/pdf-parser-*.js`
- Moved back to the next `ops` closure slice instead of starting another feature:
  - `GET /api/ops/job-runs/summary` now computes `verdict`, `reasons`, `age_minutes`, and consecutive failure counters
  - `OpsPanel` now renders the backend verdict directly instead of inferring health from the latest row alone
  - consecutive `status=error` and consecutive report-error streaks are now visible in both API response and UI
- Verification for the ops-verdict slice:
  - `npm.cmd --workspace @hearth/api run build` passed
  - `npm.cmd --workspace @hearth/api run test` passed (`99/99`)
  - `npm.cmd --workspace @hearth/web run check` passed
  - `npm.cmd --workspace @hearth/web run build` passed
- Tightened the ops-verdict contract so callers do not have to guess the health policy:
  - `GET /api/ops/job-runs/summary` now returns the threshold policy it applied
  - `daily-update` now has a default freshness policy of `4320` minutes (72h), so old successful runs do not silently stay `healthy` when callers omit `max_age_minutes`
  - `OpsPanel` now shows the active policy instead of treating the verdict as a magic constant
- Continued from the final review findings instead of leaving them as notes:
  - `OpsPanel` now catches request failures and clears loading state correctly
  - `PortfolioPanel` now clears stale `history` / `fxRates` / `tradeCosts` state when the session changes or a fresh load starts
  - portfolio routes now return `database_error` when `price_snapshots`, `fx_rates`, or holdings lookup queries fail, instead of silently degrading into guessed values
- Verification for the review-fix slice:
  - `npm.cmd --workspace @hearth/api run build`
  - `npm.cmd --workspace @hearth/api run test`
  - `npm.cmd --workspace @hearth/web run check`
  - `npm.cmd --workspace @hearth/web run build`
- Continued on the next ops closure slice instead of reopening feature work:
  - `scripts/post-deploy-smoke.ps1` now supports `-RequireOpsSummaryHealthy`
  - smoke can now fail on unhealthy recent-window summary verdicts, not only on the single latest job-run row
  - added explicit knobs for `OpsSummaryLimit`, `OpsConsecutiveFailureThreshold`, and `OpsConsecutiveReportErrorThreshold`
- Switched back to a correctness slice and tightened parser-backed preview evidence:
  - added route-level preview tests for `sinopac-stock`, `foreign-stock-csv`, and `dividends-csv`
  - fixed stock preview sample rows so the `price` column is populated from `price_per_share` instead of showing blank values
- Verification for the preview-coverage slice:
  - `npm.cmd --workspace @hearth/api run build`
  - `npm.cmd --workspace @hearth/api run test` (`105/105`)
