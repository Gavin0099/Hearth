# 2026-03-27

- Hardened CTBC PDF parsing against fragmented statement text:
  - fixed shared PDF whitespace normalization so adjacent transaction dates are no longer collapsed into malformed tokens like `02/03/02/06`
  - expanded merged-row artifact detection to reject descriptions that still contain adjacent ROC full-date pairs, allowing CTBC recovery paths to run
  - added a CTBC token-stream recovery path that can extract transactions from noisy text streams where cover-page summary fragments appear before or between transaction rows
- Added regression coverage for the newly validated CTBC failure mode:
  - parser now has a test case covering summary markers like `0800-024365`, `300,000`, and `7-ELEVEN ATM` mixed into the text stream before real CTBC transaction rows
- Verification notes:
  - `npm --workspace @hearth/api run build` passed
  - `npm --workspace @hearth/web run check` passed
  - `npm --workspace @hearth/api run test` still has pre-existing red tests in import route mocks (`supabase.from(...).upsert is not a function`); CTBC parser behavior tests and the previously failing full-width punctuation parser expectations now pass
