# 2026-05-07

## Session Summary

### 1. ai-governance-framework 完整導入（v1.2.0）

- 將 `ai-governance-framework/` 從 `a502b80`（舊 fork）reset 到 `gavin0099/main` 最新版（`78984f4`，608 commits ahead，v1.2.0）
- cherry-pick 保留 Hearth 本地 payload-audit docs commit（`88aa9a6` → `62a6d2d`）
- 執行 `adopt_governance.py`：補入新 governance docs、rule packs、AGENTS.base.md 刷新、`.governance/baseline.yaml` 更新
- 新增 `governance/framework.lock.json`（記錄 `adopted_release: v1.2.0`、`adopted_commit: 78984f4`）
- 安裝 git hooks（pre-commit plan freshness 檢查、pre-push 合規快照驗證）
- 在 `AGENTS.md` 加入四組 governance key 區段（risk_levels、must_test_paths、escalation_triggers、forbidden_behaviors）
- 最終 drift check：17/18 PASS，`ok=True`（剩 `expansion_boundary` 為 framework 自身問題）

### 2. Gmail 重新登入 & 帳單顯示問題修正

**問題**：
- 常常要重新登入：`signInWithOAuth` 未請求 `access_type: offline`，Google 不發 refresh token，1 小時後強制重登
- 永豐信用卡帳單通知看不到：`has:attachment` 過濾掉無 PDF 附件的通知信

**修正**：
- `apps/web/src/lib/auth.ts`：加入 `queryParams: { access_type: "offline", prompt: "consent" }`，確保取得 refresh token，Supabase 可自動換新 provider_token
- `apps/web/src/lib/gmail.ts`：移除 `has:attachment`、改用 `after:YYYY/MM/DD`（90 天內）過濾、`maxResults` 從 4 提升到 12
- `apps/web/src/components/GmailSyncPanel.tsx`：無 PDF 附件的通知信顯示「通知信，無 PDF 附件」標籤，匯入按鈕停用

### 3. 信用卡交易日期語意修正（繳款月）

**問題**：信用卡帳單交易依入帳日（posted date）分散在多個月，同一張帳單的 4 月和 5 月消費被歸為不同月份，無法清楚得知本月信用卡繳費總額。

**修正**：`apps/web/src/components/GmailSyncPanel.tsx` 信用卡匯入路徑，在 PDF parse 完成後，將所有交易的 date 覆寫為 `email 寄出月-01`（繳款月）。

```typescript
// Override posted dates → billing month (email 寄出月 = 繳款月)
const emailSendDate = email.date ? new Date(email.date) : new Date();
const billingMonth = `${emailSendDate.getFullYear()}-${String(emailSendDate.getMonth() + 1).padStart(2, "0")}-01`;
const billedParsed = parsed.map((tx) => ({ ...tx, date: billingMonth }));
```

**適用範圍**：所有 credit card parsers（sinopac、esun、cathay、taishin、ctbc、mega）。銀行帳戶 bank statement 路徑不受影響（仍使用入帳日）。

### 4. 股價 / 匯率 UI 可視性 + 手動觸發（Item 1 & 2）

**背景**：`apps/api/src/cron/daily-update.ts` 已在 Mon-Fri 6AM UTC 自動抓取 TWSE 股價與 ExchangeRate-API 匯率，但 UI 完全不知道 cron 狀態。

**新增**：
- `apps/api/src/routes/ops.ts`：新增 `POST /api/ops/trigger-daily-update`，需 auth，呼叫 `runDailyUpdate(env)`，回傳 `DailyUpdateReport`
- `apps/web/src/lib/ops.ts`：補入 `fetchLatestJobRun`、`triggerDailyUpdate` client functions
- `apps/web/src/components/PortfolioPanel.tsx`：mount 時抓最近 job_run 狀態、顯示「上次更新：YYYY-MM-DD HH:MM」與「立即更新股價/匯率」按鈕

### 5. 未支援銀行帳戶 Parser 提示（Item 3）

目前只有永豐、玉山 bank statement parser 驗證。在 `GmailSyncPanel.tsx` 加入：
- `VERIFIED_BANK_STATEMENT_PARSERS: BankKey[] = ["sinopac", "esun"]`
- `buildParseFailureMessage` 新增 `isBankStatement` 參數，若是未驗證銀行的對帳單則顯示明確提示

### 6. 服務端 Gmail 排程（Item 4）

**架構**：服務端 cron 每月 5 日掃描 Gmail，將待匯入帳單寫入 `gmail_sync_queue`；瀏覽器端在 GmailSyncPanel 偵測佇列後一鍵處理（PDF 下載 + parse + import 仍在 browser 執行，避免 CF Workers 不支援 pdfjs-dist）。

**新增/修改檔案**：
- `supabase/migrations/20260507000000_add_gmail_server_sync.sql`：新增 `user_settings.gmail_refresh_token` 欄位與 `gmail_sync_queue` 表（含 RLS）
- `apps/api/src/lib/secrets.ts`：`USER_SETTINGS_SECRET_FIELDS` 加入 `"gmail_refresh_token"`
- `apps/api/src/types.ts`：`WorkerBindings` 加入 `GOOGLE_CLIENT_ID?`, `GOOGLE_CLIENT_SECRET?`
- `apps/api/wrangler.jsonc`：crons 加入 `"0 2 5 * *"`（每月 5 日 2AM UTC）
- `apps/api/src/index.ts`：scheduled handler 依 `controller.cron` 分發（`"0 2 5 * *"` → `runGmailSync`，其餘 → `runDailyUpdate`）
- `apps/api/src/cron/gmail-sync.ts`：新建，掃描所有用戶 Gmail → upsert `gmail_sync_queue`
- `apps/api/src/routes/user-settings.ts`：PUT `/` 加入 `gmail_refresh_token` 加密儲存
- `apps/web/src/App.tsx`：`onAuthStateChange` SIGNED_IN 事件時自動 POST `provider_refresh_token` 到 API
- `apps/web/src/components/GmailSyncPanel.tsx`：mount 時查 `gmail_sync_queue`；若有待處理項目顯示「一鍵匯入佇列」按鈕

**環境變數（需手動設定）**：
- Cloudflare Dashboard Secrets：`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`

## Key Decisions

- Hearth 正式記錄採用 `ai-governance-framework` v1.2.0，`framework.lock.json` 為鎖定依據
- Gmail OAuth 必須帶 `access_type: offline`，否則每小時需要重登，這是長期穩定性基本要求
- Gmail 查詢策略：不以 `has:attachment` 做硬過濾，改以 90 天日期範圍限縮，讓通知信也可見但在 UI 層提示無法匯入
- **信用卡交易以繳款月（email 寄出月）為 date，而非個別入帳日**：確保同一帳單所有消費歸入同一個月，方便查看本月應繳金額
- **服務端 Gmail cron 只負責掃描 metadata，不下載 PDF**：CF Workers 不支援 pdfjs-dist（bundle size 超限），PDF 解析仍在瀏覽器執行
- `provider_refresh_token` 在 Supabase OAuth SIGNED_IN 事件中擷取，加密後儲存至 `user_settings`，供服務端 cron 每月換 access_token 掃描 Gmail

## Validation Notes

- governance drift check 通過（17/18 PASS）
- Gmail 修改為 L1 層級（import UX、無 parser 語意變更），不需要完整測試套件
- `access_type: offline` 修改需使用者重新登入一次才生效
- 信用卡繳款月修正為 L2（影響交易日期語意），tsc --noEmit PASS；用戶需重新匯入已有帳單才能更新歷史資料
- Items 1-4 全部 `tsc --noEmit` PASS（API + web 兩個 package）
- 銀行 PDF parser (Item 3) 等待 user 提供 國泰/中信/台新/兆豐 PDF 樣本；暫以 UI 提示替代
- Gmail 服務端排程需手動在 Cloudflare Dashboard 設定 `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET` secrets，以及執行 Supabase migration `20260507000000_add_gmail_server_sync.sql`
